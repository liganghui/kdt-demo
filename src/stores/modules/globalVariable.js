

// 定义 Vuex 的 state
const state = () => ({
    fontMinSize: 0,
    fontMaxSize: 99999,
    textareaMaxSize: Infinity,
})

const actions = {
}


export default {
  namespaced: false,
  state,
  actions
}